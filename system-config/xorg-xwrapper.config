# /etc/X11/Xwrapper.config

# This is needed as we start the xserver from a subshell which is not
# considered as a 'console' to Xorg.wrap
allowed_users = anybody

# While root is not needed the default of 'auto' has often made the wrong
# decision on nvidia systems
needs_root_rights = no
