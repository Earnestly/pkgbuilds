From b31e79a1df00ed946ae3f91c8f4542a2df36b47d Mon Sep 17 00:00:00 2001
From: Earnestly <zibeon@googlemail.com>
Date: Thu, 1 Nov 2018 18:39:50 +0000
Subject: [PATCH] Actually fix overlapping

---
 lib/renderers/cairo.h | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/lib/renderers/cairo.h b/lib/renderers/cairo.h
index 307b9c2..43d2f6c 100644
--- a/lib/renderers/cairo.h
+++ b/lib/renderers/cairo.h
@@ -140,7 +140,7 @@ bm_cairo_draw_line(struct cairo *cairo, struct cairo_paint *paint, struct cairo_
     cairo_fill(cairo->cr);
 
     cairo_set_source_rgba(cairo->cr, paint->fg.r, paint->fg.b, paint->fg.g, paint->fg.a);
-    cairo_move_to(cairo->cr, paint->box.lx + paint->pos.x, paint->pos.y + paint->box.ty);
+    cairo_move_to(cairo->cr, paint->box.lx + paint->pos.x, paint->pos.y + paint->box.ty - 1);
     pango_cairo_show_layout(cairo->cr, layout);
 
     g_object_unref(layout);
-- 
2.19.1

